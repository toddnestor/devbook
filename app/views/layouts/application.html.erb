<!DOCTYPE html>
<html>
  <head>
    <title>Devbook</title>
    <%= csrf_meta_tags %>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <% if current_user %>
      <script type="text/javascript">
        window.currentUser = {
          id: <%= current_user.id %>,
          username: "<%= current_user.username %>",
          lname: "<%= current_user.lname %>",
          fname: "<%= current_user.fname %>",
          email: "<%= current_user.email %>",
          tagline: "<%= current_user.tagline %>",
          avatar_url: "<%= current_user.avatar_url %>",
          cover_image_url: "<%= current_user.cover_image_url %>",
          works_at: "<%= current_user.works_at %>",
          lives_at: "<%= current_user.lives_at %>",
          hometown: "<%= current_user.hometown %>",
          birthday: "<%= current_user.birthday %>"
        };
      </script>
    <% end %>
    <%= favicon_link_tag %>
    <link rel="stylesheet" href="http://devbook.objects.cdn.dream.io/bootstrap.css" media="screen" title="no title" />
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body class="with-top-navbar">
    <%= yield %>
  </body>
</html>
