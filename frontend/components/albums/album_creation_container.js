import { connect } from 'react-redux';
import AlbumCreation from './album_editing';

const mapStateToProps = state => ({

});

const mapDispatchToProps = dispatch => ({

});

export default connect(
  mapStateToProps,
  mapDispatchToProps
)(AlbumCreation);
