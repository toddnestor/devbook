import { connect } from 'react-redux';
import AlbumEditing from './album_editing';

const mapStateToProps = state => ({

});

const mapDispatchToProps = dispatch => ({

});

export default connect(
  mapStateToProps,
  mapDispatchToProps
)(AlbumEditing);
